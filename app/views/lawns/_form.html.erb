<%= form_for(@lawn) do |f| %>
  <% if @lawn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lawn.errors.count, "error") %> prohibited this lawn from being saved:</h2>

      <ul>
      <% @lawn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :width %><br>
    <%= f.number_field :width %>
  </div>
  <div class="field">
    <%= f.label :height %><br>
    <%= f.number_field :height %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
